package com.example.bad

import com.example.Person

rule "Missing End Rule"
    when
        $person : Person(age >= 18)
    then
        System.out.println("This rule has no end");

rule InvalidRuleName
    when
        $person : Person()
    then
        System.out.println("Rule name not quoted");
end

function badFunction() {
    // Missing return type
    return "test";
}

rule "Duplicate Rule"
    when
        $p : Person()
    then
        System.out.println("First rule");
end

rule "Duplicate Rule"
    when
        $p : Person()
    then
        System.out.println("Second rule with same name");
end